<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org/ /DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ytyo.Dao.WatchHistoryMapper">
    <insert id="insertWatchHistory">
        insert into watch_history (user_id, room_id)
        values (#{userId}, #{roomId})
    </insert>
    <update id="updateWatchHistoryLastWatchTime">
        update watch_history
        set last_watch_time=#{lastWatchTime}
        where user_id = #{userId}
          and room_id = #{roomId}
    </update>

    <insert id="updateOrInsertHistory">
        INSERT INTO watch_history (room_id, user_id, last_watch_time)
        VALUES (#{roomId}, #{userId}, #{lastWatchTime}) ON DUPLICATE KEY
        UPDATE
            last_watch_time =
        VALUES (last_watch_time)
    </insert>

    <delete id="deleteWatchHistory">
        delete
        from watch_history
        where user_id = #{userId}
          and room_id = #{roomId}
    </delete>

    <select id="selectWatchHistoryByUserId" resultType="com.ytyo.Model.WatchHistory">
        select *
        from watch_history
        where user_id = #{userId}
    </select>

</mapper>